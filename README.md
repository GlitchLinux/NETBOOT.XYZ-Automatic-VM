# NETBOOT.XYZ-Automatic-VM
This Bash script automates setting up and launching a NETBOOT.XYZ virtual machine using QEMU. It requires root access, cleans the `/tmp` directory, installs necessary packages, and downloads the `netboot.xyz` image. The user specifies the RAM amount in MB, and the script starts the VM with that configuration using KVM acceleration, 
